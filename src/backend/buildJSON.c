#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

char *buildCPUJSON() {
    char *result = malloc(); // allocate space on the heap for the JSON object



    return result;
}

char *buildDiskJSON() {
    char *result = (char *)malloc(result * sizeof(char)); // allocate space on the heap for the JSON object



    return result;
}

char *buildMemoryJSON() {
    char *result = malloc(); // allocate space on the heap for the JSON object



    return result;
}
